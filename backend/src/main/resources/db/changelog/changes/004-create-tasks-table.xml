<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.20.xsd">

    <!--
        Adjust the changeSet id/author, and the numeric prefix on this
        file's name, to fit your existing changelog sequence. Confirm the
        "projects" and "users" table names below match your other
        changesets exactly.
    -->
    <changeSet id="005-create-tasks-table" author="claude">

        <createTable tableName="tasks">

            <column name="id" type="BIGINT" autoIncrement="true">
                <constraints primaryKey="true" nullable="false"/>
            </column>

            <column name="title" type="VARCHAR(255)">
                <constraints nullable="false"/>
            </column>

            <column name="description" type="VARCHAR(1000)">
                <constraints nullable="true"/>
            </column>

            <column name="project_id" type="BIGINT">
                <constraints nullable="false"/>
            </column>

            <column name="created_by" type="BIGINT">
                <constraints nullable="false"/>
            </column>

            <column name="assigned_to" type="BIGINT">
                <constraints nullable="true"/>
            </column>

            <column name="status" type="VARCHAR(30)" defaultValue="TODO">
                <constraints nullable="false"/>
            </column>

            <column name="start_date" type="DATE">
                <constraints nullable="true"/>
            </column>

            <column name="due_date" type="DATE">
                <constraints nullable="true"/>
            </column>

            <column name="created_at" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>

            <column name="updated_at" type="TIMESTAMP">
                <constraints nullable="false"/>
            </column>

        </createTable>

        <addForeignKeyConstraint
                baseTableName="tasks"
                baseColumnNames="project_id"
                constraintName="fk_tasks_project"
                referencedTableName="projects"
                referencedColumnNames="id"
                onDelete="CASCADE"/>

        <addForeignKeyConstraint
                baseTableName="tasks"
                baseColumnNames="created_by"
                constraintName="fk_tasks_created_by"
                referencedTableName="users"
                referencedColumnNames="id"
                onDelete="RESTRICT"/>

        <addForeignKeyConstraint
                baseTableName="tasks"
                baseColumnNames="assigned_to"
                constraintName="fk_tasks_assigned_to"
                referencedTableName="users"
                referencedColumnNames="id"
                onDelete="SET NULL"/>

        <createIndex indexName="idx_tasks_project_id" tableName="tasks">
            <column name="project_id"/>
        </createIndex>

        <createIndex indexName="idx_tasks_assigned_to" tableName="tasks">
            <column name="assigned_to"/>
        </createIndex>

    </changeSet>

</databaseChangeLog>
